import { Route } from '@angular/router';
import { CharacterGridComponent } from './character-grid/character-grid.component';

export const characterGridRoutes: Route[] = [
  { path: '', component: CharacterGridComponent },
];
